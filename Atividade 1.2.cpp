//Faça um programa que calcule a soma dos n primeiros numeros primos, sendo n definido pelo usuário.
//(Utilize laço FOR ou WHILE)
#include <stdio.h>
int main(){
	int n=0, cont=1;
	
	printf("Digite quantos termos divisiveis por tres devem ser somados: ");
	scanf("%d", &n);
	
	while(<=n){
		if(cont%3==0)
			
	}
}
